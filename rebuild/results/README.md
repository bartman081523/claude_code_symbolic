# 2-stage/symbolic extension — live experiment results

Generated by running the patched binary (`out/claude.patched`, 3-opt-in flags
ENABLED/SYMBOLIC/SCIMIND) against Ollama Cloud. All tiers `minimax-m3:cloud`
unless noted.

## chemistry/ — 8-combo chemistry answers (run8.log)
Question (verbatim): "Eine bestimmte chemische Reaktion umfasst drei Schritte:
zwei Elektrocyclisierungen gefolgt von einer Cycloaddition. …"
Answers from all 8 (E,S,C) combos. Trace-confirmed path per combo
(stock / 2-stage / symbolic).

## tool-task/ — "Trapping Rain Water" (hard, compact, tool-use)
spec.md + test.py (12 cases). All 8 combos solved it (ALL TESTS PASSED),
each via Read→Write→Bash. solution-<combo>.py per combo. 8-combo-run.log has
the transcript tool-use counts + stage signals. Demonstrates extensions drive
tool calls identically to stock.

## hard-task/ — "Cherry Pickup" (LeetCode 741, classic greedy trap)
spec.md + test.py (18 cases, 6 naive-breaking). All 8 combos solved it
(genuine simultaneous two-path DP, no cheating). solution-<combo>.py per
combo. 8-combo-run.log has duration/turns/stage signals.

## generated-tasks/ — 8 hard tasks authored by the 8 combos (2-min thinking)
Each combo designed its own "quick but insanely hard" task that stock and
glm-5.2 should NOT be able to solve. See generated-tasks/<combo>/.
